# .env.example - Copy to .env and fill in your actual values
# This file is for example purposes and should be in version control.
# The .env file itself (containing actual secrets) should be in .gitignore.

# eBay API Credentials (obtain from your eBay Developer Program account)
EBAY_CLIENT_ID="YOUR_EBAY_APP_ID_OR_CLIENT_ID_HERE"
EBAY_CLIENT_SECRET="YOUR_EBAY_CERT_ID_OR_CLIENT_SECRET_HERE"

# Flask Secret Key (generate a strong random key)
# For example, in Python: import secrets; secrets.token_hex(32)
SECRET_KEY="YOUR_RANDOMLY_GENERATED_FLASK_SECRET_KEY_HERE"

# Flask Environment (development or production)
FLASK_ENV="development"

# Optional: If you have a separate DEV ID for eBay (not typically used with OAuth Client Credentials)
# EBAY_DEV_ID="YOUR_EBAY_DEV_ID_IF_NEEDED"

# ── Mailgun ───────────────────────────────────────────
MAILGUN_API_KEY=your-mailgun-private-key
MAILGUN_DOMAIN=mg.yourdomain.com      # the domain you verified in Mailgun